import React from 'react';


const ReceiverChild = (props) => {
    // console.log(props);
    return(
        <div>
            <h1>{props.state}</h1>
        </div>
    )
};

export default ReceiverChild;